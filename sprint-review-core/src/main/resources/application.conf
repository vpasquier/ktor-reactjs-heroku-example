ktor {
    deployment {
        environment = development
        port = ${PORT}
        watch = [ hello ]
        autoreload = true
    }

    application {
        modules = [ com.sprintreview.TomcatMainKt.main ]
    }
}
ktor {
    deployment {
        environment = development
        port = ${PORT}
        watch = [ sprint-review-core ]
        autoreload = true
    }

    application {
        modules = [ com.sprintreview.ServerKt.server ]
    }
}